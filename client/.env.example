# WebSocket server URL the client connects to.
# If unset, the client falls back to ws://localhost:8080 (local dev).
#
# In production, set this to your deployed server's wss:// URL, e.g.:
#   VITE_WS_URL=wss://collab-whiteboard-server.onrender.com
VITE_WS_URL=ws://localhost:8080
